# InfraFlow 프로젝트 개선 계획서

> **문서 버전**: 1.0.0
> **작성일**: 2026-02-07
> **작성자**: Claude Code Review Agent
> **대상**: InfraFlow v0.1.0-alpha

---

## 목차

1. [Executive Summary](#1-executive-summary)
2. [현재 프로젝트 현황](#2-현재-프로젝트-현황)
3. [코드 분석 결과](#3-코드-분석-결과)
4. [비관 에이전트 피드백](#4-비관-에이전트-피드백-pessimist-agent)
5. [낙관 에이전트 피드백](#5-낙관-에이전트-피드백-optimist-agent)
6. [종합 분석 및 우선순위화](#6-종합-분석-및-우선순위화)
7. [개선 PR 계획](#7-개선-pr-계획)
8. [병렬 처리 다이어그램](#8-병렬-처리-다이어그램)
9. [체크리스트](#9-체크리스트)

---

## 1. Executive Summary

### 프로젝트 개요
InfraFlow는 자연어 프롬프트로 인프라 아키텍처를 시각화하는 플랫폼입니다. Next.js 16 + React Flow 기반으로 구현되었으며, LLM을 활용한 프롬프트 파싱, 애니메이션 데이터 흐름, 보안 감사, 비용 산출 기능을 제공합니다.

### 현재 상태 요약

| 지표 | 값 | 상태 |
|------|-----|------|
| 총 소스 파일 | 141개 | ✅ 양호 |
| 코드 라인 수 | 34,538 LOC | ✅ 적정 |
| 테스트 커버리지 | ~30-40% | ⚠️ 개선 필요 |
| 중복 코드 비율 | ~15% | ⚠️ 리팩토링 필요 |
| 번들 크기 (추정) | ~500KB | ⚠️ 최적화 가능 |

### 핵심 개선 필요 영역

1. **테스트 커버리지 확대** (현재 30% → 목표 80%)
2. **코드 중복 제거** (파서 모듈, 노드 컴포넌트)
3. **성능 최적화** (메모이제이션, 지연 로딩)
4. **접근성 개선** (ARIA 라벨, 키보드 네비게이션)

---

## 2. 현재 프로젝트 현황

### 2.1 디렉토리 구조

```
src/
├── __tests__/              # 테스트 파일 (11개, 1,625 LOC)
├── app/                    # Next.js App Router (15개)
├── components/             # React 컴포넌트 (46개)
│   ├── nodes/             # 24개 인프라 노드 컴포넌트
│   ├── panels/            # 12개 UI 패널
│   ├── admin/             # 관리자 인터페이스
│   ├── edges/             # 애니메이션 엣지
│   └── shared/            # 공통 컴포넌트
├── lib/                    # 비즈니스 로직 (75개+)
│   ├── parser/            # 5개 파서 구현체
│   ├── export/            # 6개 내보내기 형식
│   ├── audit/             # 4개 감사/규정 준수 모듈
│   ├── animation/         # 애니메이션 엔진
│   ├── cost/              # 비용 산출기
│   └── templates/         # 템플릿 관리
├── hooks/                  # 커스텀 훅 (14개, 2,396 LOC)
└── types/                  # TypeScript 정의 (3개)
```

### 2.2 주요 파일 크기 분석

| 파일 | 라인 수 | 위험도 | 비고 |
|------|---------|--------|------|
| `infrastructureDB.ts` | 1,519 | ⚠️ 높음 | 분리 필요 |
| `complianceChecker.ts` | 659 | 중간 | 헬퍼 함수 분리 권장 |
| `terraformExport.ts` | 604 | 중간 | 템플릿 파일 분리 고려 |
| `intelligentParser.ts` | 547 | 중간 | 정상 범위 |
| `SecurityAuditPanel.tsx` | 465 | 중간 | 탭 컴포넌트 분리 권장 |

### 2.3 의존성 현황

**프로덕션 의존성 (12개)**:
- 프레임워크: Next.js 16.1.6, React 19.2.3
- 다이어그램: @xyflow/react 12.10.0
- 애니메이션: framer-motion 12.31.0
- 내보내기: html2canvas 1.4.1, jspdf 4.1.0
- 데이터: Prisma 6.19.2, SWR 2.4.0

**상태**: ✅ 의존성 구성이 적절함

---

## 3. 코드 분석 결과

### 3.1 중복 코드 발견

#### A. 파서 모듈 중복 (심각도: 높음)

**위치**: `src/lib/parser/`

| 파일 | 중복 내용 |
|------|----------|
| `promptParser.ts` (67-84줄) | 컴포넌트 패턴 정의 |
| `smartParser.ts` (100-104, 180-187, 250-254, 312-316줄) | 패턴 매칭 루프 4회 반복 |
| `patterns.ts` (27줄) | 동일 패턴의 원본 정의 |

**영향**: 패턴 추가 시 2-3곳 동시 수정 필요, 불일치 위험

**해결책**:
```typescript
// patterns.ts에 통합 함수 추가
export function detectComponentsInPrompt(
  prompt: string,
  patterns: NodeTypePattern[] = nodeTypePatterns
): Array<{ type: InfraNodeType; label: string }> {
  return patterns
    .filter(({ pattern }) => pattern.test(prompt))
    .map(({ type, label }) => ({ type, label }));
}
```

#### B. 노드 컴포넌트 중복 (심각도: 높음)

**위치**: `src/components/nodes/`

- `SecurityNodes.tsx`: 6개 동일 패턴
- `ComputeNodes.tsx`: 6개 동일 패턴
- `NetworkNodes.tsx`: 7개 동일 패턴
- `ExternalNodes.tsx`: 8개 동일 패턴

**현재 패턴** (23회 반복):
```tsx
export const FirewallNode = memo(function FirewallNode({ data, selected }: InfraNodeProps) {
  const editProps = useEditableNode();
  return <BaseNode data={data} icon="🛡️" color="red" selected={selected} {...editProps} />;
});
```

**해결책**: 팩토리 함수 도입
```typescript
// nodeFactory.ts
export function createInfraNode(config: NodeConfig) {
  return memo(function InfraNode({ data, selected }: InfraNodeProps) {
    const editProps = useEditableNode();
    return <BaseNode data={data} icon={config.icon} color={config.color} selected={selected} {...editProps} />;
  });
}

// 사용
export const FirewallNode = createInfraNode({ icon: '🛡️', color: 'red' });
```

#### C. 스타일 상수 중복 (심각도: 중간)

| 파일 | 정의 |
|------|------|
| `BaseNode.tsx` (25-79줄) | `categoryStyles` |
| `NodeDetailPanel.tsx` (23-31줄) | `categoryColors` |
| `ComponentTable.tsx` (66-81줄) | `categoryColors`, `tierColors` |

**해결책**: `src/lib/design/tokens.ts`로 통합

### 3.2 성능 문제

#### A. 정규식 반복 컴파일

**위치**: `smartParser.ts`, `promptParser.ts`

```typescript
// 매 함수 호출마다 30+ 패턴 테스트 반복
for (const { pattern, type, label } of nodeTypePatterns) {
  if (pattern.test(prompt)) { ... }
}
```

**해결책**: 패턴 매칭 결과 캐싱
```typescript
const patternCache = new Map<string, InfraNodeType[]>();

export function detectComponentsCached(prompt: string): InfraNodeType[] {
  const key = prompt.toLowerCase();
  if (patternCache.has(key)) return patternCache.get(key)!;

  const matches = nodeTypePatterns
    .filter(p => p.pattern.test(key))
    .map(p => p.type);

  patternCache.set(key, matches);
  return matches;
}
```

#### B. 레이아웃 엔진 O(n²) 정렬

**위치**: `layoutEngine.ts` (193-226줄)

```typescript
// 각 노드마다 연결 수 계산 반복
const aConnections = (adjacency.get(a.id)?.length || 0) +
                    (reverseAdjacency.get(a.id)?.length || 0);
```

**해결책**: 연결 수 사전 계산
```typescript
// 정렬 전 연결 수 맵 생성
const connectionCounts = new Map<string, number>();
for (const node of nodes) {
  connectionCounts.set(node.id,
    (adjacency.get(node.id)?.length || 0) +
    (reverseAdjacency.get(node.id)?.length || 0)
  );
}
// 정렬 시 O(1) 조회
nodes.sort((a, b) => connectionCounts.get(b.id)! - connectionCounts.get(a.id)!);
```

#### C. 메모리 누수 위험

**위치**: `animationEngine.ts` (183-186줄)

파티클 정리 로직이 타이밍에 의존하여 실패 시 메모리 누적 가능

**해결책**: 타임스탬프 기반 정리 추가
```typescript
const MAX_PARTICLE_AGE = 60000; // 60초
const now = Date.now();
for (const [edgeId, particles] of this.state.activeFlows.entries()) {
  const active = particles.filter(p => now - p.startTime < MAX_PARTICLE_AGE);
  if (active.length === 0) {
    this.state.activeFlows.delete(edgeId);
  } else {
    this.state.activeFlows.set(edgeId, active);
  }
}
```

### 3.3 타입 안전성 문제

#### A. 안전하지 않은 캐스팅

**위치**: `layoutEngine.ts` (316-321줄)

```typescript
const data = n.data as InfraNodeData;  // 검증 없는 캐스팅
return {
  type: data.nodeType,  // undefined 가능
  label: data.label,
};
```

**해결책**: 타입 가드 추가
```typescript
function isValidNodeData(data: unknown): data is InfraNodeData {
  return typeof data === 'object' && data !== null &&
    'nodeType' in data && 'label' in data;
}

// 사용
if (!isValidNodeData(n.data)) {
  throw new Error(`Invalid node data for node ${n.id}`);
}
```

### 3.4 접근성 문제

#### 발견된 이슈

| 컴포넌트 | 이슈 | 위치 |
|----------|------|------|
| `EditableLabel.tsx` | 한국어 title만 존재, aria-label 없음 | 125줄 |
| `ConversationPanel.tsx` | 클릭 가능한 div에 role 없음 | 220-230줄 |
| `NodeDetailPanel.tsx` | 아코디언에 aria-expanded 없음 | 144-184줄 |
| `ComponentTable.tsx` | 액션 버튼에 aria-label 없음 | 196-203줄 |

### 3.5 테스트 커버리지 격차

| 모듈 | 테스트 | 상태 |
|------|--------|------|
| `hooks/` | ✅ 있음 | 6개 파일 테스트됨 |
| `lib/parser/` | ✅ 있음 | 3개 파일 테스트됨 |
| `lib/layout/` | ✅ 있음 | 1개 파일 테스트됨 |
| `components/` | ❌ 없음 | 46개 파일 미테스트 |
| `app/api/` | ❌ 없음 | 4개 API 라우트 미테스트 |
| `lib/export/` | ❌ 없음 | 6개 파일 미테스트 |
| `lib/audit/` | ❌ 없음 | 4개 파일 미테스트 |

---

## 4. 비관 에이전트 피드백 (Pessimist Agent)

### 🔴 Critical Risks (즉시 해결 필요)

#### Risk 1: 테스트 부재로 인한 회귀 위험
> **심각도**: Critical
> **확률**: 90%
> **영향**: 기능 변경 시 기존 기능 파손 가능

**분석**: 현재 테스트 커버리지 30-40%로, 컴포넌트와 API 라우트가 완전히 미테스트 상태입니다. 리팩토링 시 회귀 버그 발생 확률이 매우 높습니다.

**최악의 시나리오**:
- PR 병합 후 프로덕션에서 핵심 기능 장애 발생
- 사용자 데이터 손실 또는 잘못된 다이어그램 생성

**권장 조치**:
1. 리팩토링 전 기존 기능에 대한 스냅샷 테스트 추가
2. CI/CD에 최소 커버리지 임계값(60%) 설정

---

#### Risk 2: 메모리 누수로 인한 브라우저 크래시
> **심각도**: High
> **확률**: 60%
> **영향**: 장시간 사용 시 브라우저 응답 없음

**분석**: `animationEngine.ts`의 파티클 시스템이 정상 종료되지 않을 경우 파티클이 무한 누적됩니다. 테스트에서 발견되지 않은 엣지 케이스가 존재할 가능성이 높습니다.

**최악의 시나리오**:
- 30분 이상 애니메이션 실행 시 메모리 2GB+ 사용
- 탭 강제 종료로 작업 손실

**권장 조치**:
1. 파티클 최대 수명 60초 제한
2. 메모리 사용량 모니터링 추가

---

#### Risk 3: LLM API 비용 폭증
> **심각도**: High
> **확률**: 70%
> **영향**: 예상치 못한 API 비용 청구

**분석**: `llm/route.ts`에 rate limiting이 없습니다. 악의적 사용자가 API를 무한 호출할 수 있습니다.

**최악의 시나리오**:
- 하루 API 비용 $1,000+ 발생
- Anthropic API 한도 초과로 서비스 중단

**권장 조치**:
1. IP 기반 rate limiting 추가 (분당 10회)
2. 일일 사용량 상한 설정

---

### 🟠 High Risks (1주 내 해결 권장)

#### Risk 4: 인프라DB 유지보수 불가
> 1,519줄의 하드코딩된 데이터는 유지보수 악몽입니다. 장비 추가/수정 시 4개 파일 동시 수정 필요.

#### Risk 5: 파서 중복으로 인한 불일치
> 3개의 파서 구현체가 각각 다른 패턴을 사용할 경우 동일 입력에 다른 결과 반환 가능.

#### Risk 6: 번들 크기 최적화 부재
> 노드 컴포넌트 23개가 거의 동일한 코드로 ~15KB 낭비. 지연 로딩 미적용.

---

### 🟡 Medium Risks (1개월 내 해결 권장)

#### Risk 7: 접근성 규정 위반
> WCAG 2.1 Level AA 미충족. 공공기관 서비스의 경우 법적 문제 가능.

#### Risk 8: 에러 핸들링 불완전
> API 에러가 사용자에게 "알 수 없는 오류"로만 표시됨.

#### Risk 9: 타입 안전성 취약점
> 런타임 캐스팅 오류 발생 시 앱 전체 크래시 가능.

---

### 비관적 예측 요약

```
┌─────────────────────────────────────────────────────────────┐
│  현재 상태로 3개월 운영 시 예상 문제                          │
├─────────────────────────────────────────────────────────────┤
│  • 주요 버그 발생: 5-10건                                    │
│  • 메모리 관련 이슈: 2-3건                                   │
│  • API 비용 초과: 1-2회                                      │
│  • 사용자 불만 접수: 20-30건                                 │
│  • 개발 생산성 저하: 30%                                     │
└─────────────────────────────────────────────────────────────┘
```

---

## 5. 낙관 에이전트 피드백 (Optimist Agent)

### 🟢 Major Strengths (핵심 강점)

#### Strength 1: 견고한 아키텍처 기반
> **가치**: 매우 높음
> **확장성**: 우수

**분석**: Next.js 16 + React Flow + TypeScript 조합은 현대적이고 확장 가능한 스택입니다. 모듈 분리가 잘 되어 있어 각 기능을 독립적으로 개선할 수 있습니다.

**기회**:
- 마이크로프론트엔드로 확장 용이
- 서버 컴포넌트 활용으로 성능 최적화 가능
- 점진적 마이그레이션 가능

---

#### Strength 2: 풍부한 기능 세트
> **가치**: 높음
> **차별화**: 우수

**분석**: 경쟁 제품 대비 독보적인 기능 조합:
- 자연어 → 다이어그램 변환 (LLM 통합)
- 실시간 데이터 흐름 애니메이션
- 6개 규정 프레임워크 준수 확인
- 4개 클라우드 비용 산출
- 다양한 내보내기 형식 (Terraform, K8s, PlantUML, PDF)

**기회**:
- B2B SaaS 제품으로 상용화 가능
- 엔터프라이즈 기능 추가 여지

---

#### Strength 3: 한국어 우선 설계
> **가치**: 높음
> **시장 차별화**: 매우 우수

**분석**: `infrastructureDB.ts`의 이중 언어 지원, 한국 규정 (ISMS-P, K-ISMS) 내장은 국내 시장에서 큰 강점입니다.

**기회**:
- 공공기관/대기업 SI 시장 진출
- 경기도의회 등 레퍼런스 확보
- 한국 클라우드 MSP 파트너십

---

#### Strength 4: 컴포넌트 라이브러리 완성도
> **가치**: 높음

**분석**: 24개의 인프라 노드 컴포넌트와 14개의 빌트인 템플릿은 즉시 사용 가능한 수준입니다. 사용자가 프롬프트만 입력하면 전문적인 다이어그램을 얻을 수 있습니다.

---

### 🌟 Growth Opportunities (성장 기회)

#### Opportunity 1: AI 기능 고도화
> 현재 LLM은 프롬프트 파싱에만 사용됩니다. 확장 가능:
> - 아키텍처 최적화 제안
> - 보안 취약점 자동 수정
> - 비용 절감 추천
> - 자연어 질의응답

#### Opportunity 2: 실시간 협업 기능
> React Flow는 멀티플레이어 협업을 지원합니다. Liveblocks/Yjs 통합으로 실시간 협업 가능.

#### Opportunity 3: 플러그인 생태계
> 노드 타입, 내보내기 형식, 규정 프레임워크를 플러그인으로 확장 가능한 구조입니다.

#### Opportunity 4: 클라우드 연동
> AWS/Azure/GCP API와 연동하여:
> - 실제 인프라 상태 반영
> - 비용 실시간 조회
> - 리소스 프로비저닝

---

### 낙관적 예측 요약

```
┌─────────────────────────────────────────────────────────────┐
│  개선 작업 완료 시 예상 효과 (6개월 후)                       │
├─────────────────────────────────────────────────────────────┤
│  • 테스트 커버리지: 80%+ (안정성 향상)                       │
│  • 번들 크기: 30% 감소 (로딩 속도 향상)                      │
│  • 개발 생산성: 50% 향상 (코드 중복 제거)                    │
│  • 사용자 만족도: 90%+ (접근성/UX 개선)                      │
│  • 엔터프라이즈 레디: 완료                                   │
└─────────────────────────────────────────────────────────────┘
```

---

## 6. 종합 분석 및 우선순위화

### 6.1 피드백 종합 매트릭스

| 이슈 | 비관 | 낙관 | 최종 우선순위 |
|------|------|------|--------------|
| 테스트 커버리지 | Critical Risk | 안정성 기반 확보 | **P0** |
| 파서 중복 | High Risk | 리팩토링 용이 | **P1** |
| 노드 팩토리 | 번들 낭비 | 유지보수성 향상 | **P1** |
| Rate Limiting | API 비용 폭증 | 상용화 필수 | **P1** |
| 메모리 누수 | 크래시 위험 | 빠른 수정 가능 | **P1** |
| 스타일 통합 | 불일치 위험 | 디자인 시스템화 | **P2** |
| 접근성 | 법적 위험 | 시장 확대 | **P2** |
| 성능 최적화 | 사용성 저하 | UX 향상 | **P2** |
| 타입 안전성 | 런타임 오류 | 안정성 향상 | **P3** |
| InfraDB 마이그레이션 | 유지보수 부담 | DB 활용 | **P3** |

### 6.2 우선순위 정의

| 우선순위 | 의미 | 기간 |
|----------|------|------|
| **P0** | 즉시 해결 필수 | 1주 내 |
| **P1** | 높은 우선순위 | 2주 내 |
| **P2** | 중간 우선순위 | 1개월 내 |
| **P3** | 낮은 우선순위 | 분기 내 |

---

## 7. 개선 PR 계획

### Phase 1: Foundation (P0-P1) - 2주

#### PR #1: 테스트 인프라 구축
**목표**: 테스트 커버리지 기반 확보

**작업 내용**:
- [ ] Vitest 컴포넌트 테스트 설정
- [ ] React Testing Library 통합
- [ ] API 라우트 테스트 유틸리티
- [ ] CI/CD 커버리지 리포트 추가
- [ ] 커버리지 임계값 설정 (60%)

**파일**:
```
vitest.config.ts (수정)
src/__tests__/setup.ts (수정)
src/__tests__/utils/renderWithProviders.tsx (신규)
src/__tests__/utils/apiTestUtils.ts (신규)
.github/workflows/test.yml (신규 또는 수정)
```

**의존성**: 없음 (독립 실행)
**예상 작업량**: 1일

---

#### PR #2: 핵심 컴포넌트 테스트
**목표**: 주요 UI 컴포넌트 테스트 추가

**작업 내용**:
- [ ] FlowCanvas 테스트
- [ ] BaseNode 테스트
- [ ] PromptPanel 테스트
- [ ] NodeDetailPanel 테스트

**파일**:
```
src/__tests__/components/FlowCanvas.test.tsx (신규)
src/__tests__/components/BaseNode.test.tsx (신규)
src/__tests__/components/PromptPanel.test.tsx (신규)
src/__tests__/components/NodeDetailPanel.test.tsx (신규)
```

**의존성**: PR #1
**예상 작업량**: 2일

---

#### PR #3: API 라우트 테스트
**목표**: API 엔드포인트 테스트 추가

**작업 내용**:
- [ ] /api/llm 테스트 (모킹)
- [ ] /api/parse 테스트
- [ ] /api/components CRUD 테스트

**파일**:
```
src/__tests__/api/llm.test.ts (신규)
src/__tests__/api/parse.test.ts (신규)
src/__tests__/api/components.test.ts (신규)
```

**의존성**: PR #1
**예상 작업량**: 1.5일

---

#### PR #4: 파서 통합 리팩토링
**목표**: 3개 파서의 중복 제거

**작업 내용**:
- [ ] `detectComponentsInPrompt()` 통합 함수 생성
- [ ] promptParser.ts 중복 패턴 제거
- [ ] smartParser.ts 패턴 루프 통합
- [ ] 패턴 매칭 결과 캐싱 추가

**파일**:
```
src/lib/parser/patterns.ts (수정)
src/lib/parser/promptParser.ts (수정)
src/lib/parser/smartParser.ts (수정)
src/__tests__/lib/parser/patterns.test.ts (수정)
```

**의존성**: 없음 (독립 실행)
**예상 작업량**: 1일

---

#### PR #5: Rate Limiting 추가
**목표**: API 보호 및 비용 관리

**작업 내용**:
- [ ] Rate limiter 미들웨어 구현
- [ ] IP 기반 제한 (분당 10회)
- [ ] 일일 사용량 상한
- [ ] 429 응답 핸들링

**파일**:
```
src/lib/middleware/rateLimiter.ts (신규)
src/app/api/llm/route.ts (수정)
src/lib/errors/RateLimitError.ts (신규)
```

**의존성**: 없음 (독립 실행)
**예상 작업량**: 0.5일

---

#### PR #6: 메모리 누수 수정
**목표**: 애니메이션 엔진 안정화

**작업 내용**:
- [ ] 파티클 최대 수명 제한 (60초)
- [ ] 타임스탬프 기반 정리 로직
- [ ] 메모리 사용량 로깅 (개발 모드)

**파일**:
```
src/lib/animation/animationEngine.ts (수정)
```

**의존성**: 없음 (독립 실행)
**예상 작업량**: 0.5일

---

### Phase 2: Optimization (P1-P2) - 2주

#### PR #7: 노드 팩토리 리팩토링
**목표**: 23개 노드 컴포넌트 통합

**작업 내용**:
- [ ] `createInfraNode()` 팩토리 함수 생성
- [ ] 노드 설정 객체 분리
- [ ] 기존 노드 컴포넌트 교체
- [ ] 테스트 추가

**파일**:
```
src/components/nodes/nodeFactory.tsx (신규)
src/components/nodes/nodeConfigs.ts (신규)
src/components/nodes/SecurityNodes.tsx (대폭 수정)
src/components/nodes/ComputeNodes.tsx (대폭 수정)
src/components/nodes/NetworkNodes.tsx (대폭 수정)
src/components/nodes/ExternalNodes.tsx (대폭 수정)
```

**의존성**: PR #2
**예상 작업량**: 1.5일

---

#### PR #8: 스타일 토큰 통합
**목표**: 중복 스타일 상수 제거

**작업 내용**:
- [ ] `design/tokens.ts` 확장
- [ ] categoryColors, tierColors, priorityColors 통합
- [ ] 컴포넌트에서 토큰 임포트로 교체

**파일**:
```
src/lib/design/tokens.ts (수정)
src/components/nodes/BaseNode.tsx (수정)
src/components/panels/NodeDetailPanel.tsx (수정)
src/components/admin/ComponentTable.tsx (수정)
```

**의존성**: 없음 (독립 실행)
**예상 작업량**: 0.5일

---

#### PR #9: NodeDetailPanel 분리
**목표**: 대형 컴포넌트 분할

**작업 내용**:
- [ ] OverviewTab 컴포넌트 분리
- [ ] FunctionsTab 컴포넌트 분리
- [ ] PoliciesTab 컴포넌트 분리
- [ ] TechnicalTab 컴포넌트 분리

**파일**:
```
src/components/panels/NodeDetailPanel/index.tsx (신규)
src/components/panels/NodeDetailPanel/OverviewTab.tsx (신규)
src/components/panels/NodeDetailPanel/FunctionsTab.tsx (신규)
src/components/panels/NodeDetailPanel/PoliciesTab.tsx (신규)
src/components/panels/NodeDetailPanel/TechnicalTab.tsx (신규)
src/components/panels/NodeDetailPanel.tsx (삭제)
```

**의존성**: PR #8
**예상 작업량**: 1일

---

#### PR #10: 성능 최적화
**목표**: 레이아웃 엔진 및 캐싱

**작업 내용**:
- [ ] 연결 수 사전 계산
- [ ] 레이아웃 결과 메모이제이션
- [ ] React.memo 적용 확인

**파일**:
```
src/lib/layout/layoutEngine.ts (수정)
src/hooks/useNodes.ts (수정)
```

**의존성**: PR #4
**예상 작업량**: 1일

---

#### PR #11: 접근성 개선
**목표**: WCAG 2.1 AA 준수

**작업 내용**:
- [ ] ARIA 라벨 추가
- [ ] 키보드 네비게이션 개선
- [ ] 포커스 관리 개선
- [ ] 스크린 리더 테스트

**파일**:
```
src/components/nodes/EditableLabel.tsx (수정)
src/components/panels/ConversationPanel.tsx (수정)
src/components/panels/NodeDetailPanel/*.tsx (수정)
src/components/admin/ComponentTable.tsx (수정)
src/components/contextMenu/*.tsx (수정)
```

**의존성**: PR #9
**예상 작업량**: 1.5일

---

### Phase 3: Enhancement (P2-P3) - 2주

#### PR #12: 타입 안전성 강화
**목표**: 런타임 오류 예방

**작업 내용**:
- [ ] 타입 가드 함수 추가
- [ ] 안전하지 않은 캐스팅 수정
- [ ] Zod 스키마 일관성 확보

**파일**:
```
src/types/guards.ts (수정)
src/lib/layout/layoutEngine.ts (수정)
src/lib/parser/intelligentParser.ts (수정)
```

**의존성**: 없음 (독립 실행)
**예상 작업량**: 1일

---

#### PR #13: 에러 핸들링 고도화
**목표**: 사용자 친화적 에러 표시

**작업 내용**:
- [ ] 에러 바운더리 개선
- [ ] API 에러 메시지 한국어화
- [ ] 재시도 로직 개선

**파일**:
```
src/components/shared/ErrorBoundary.tsx (수정)
src/lib/errors/*.ts (수정)
src/hooks/useErrorHandler.ts (수정)
```

**의존성**: 없음 (독립 실행)
**예상 작업량**: 1일

---

#### PR #14: Export 모듈 테스트
**목표**: 내보내기 기능 안정화

**작업 내용**:
- [ ] terraformExport 테스트
- [ ] kubernetesExport 테스트
- [ ] plantUMLExport 테스트
- [ ] pdfReportGenerator 테스트

**파일**:
```
src/__tests__/lib/export/terraformExport.test.ts (신규)
src/__tests__/lib/export/kubernetesExport.test.ts (신규)
src/__tests__/lib/export/plantUMLExport.test.ts (신규)
src/__tests__/lib/export/pdfReportGenerator.test.ts (신규)
```

**의존성**: PR #1
**예상 작업량**: 2일

---

#### PR #15: Audit 모듈 테스트
**목표**: 보안/규정 기능 안정화

**작업 내용**:
- [ ] securityAudit 테스트
- [ ] complianceChecker 테스트
- [ ] auditReportGenerator 테스트

**파일**:
```
src/__tests__/lib/audit/securityAudit.test.ts (신규)
src/__tests__/lib/audit/complianceChecker.test.ts (신규)
src/__tests__/lib/audit/auditReportGenerator.test.ts (신규)
```

**의존성**: PR #1
**예상 작업량**: 1.5일

---

#### PR #16: Deprecated 코드 제거
**목표**: 기술 부채 정리

**작업 내용**:
- [ ] getAnimationEngine 제거
- [ ] resetAnimationEngine 제거
- [ ] 미사용 함수 정리

**파일**:
```
src/lib/animation/animationEngine.ts (수정)
src/lib/animation/index.ts (수정)
```

**의존성**: PR #6
**예상 작업량**: 0.5일

---

## 8. 병렬 처리 다이어그램

```
시간 ────────────────────────────────────────────────────────────────────▶

Week 1                              Week 2
┌─────────────────────────────────┐ ┌─────────────────────────────────┐
│                                 │ │                                 │
│  Stream A (Testing Foundation)  │ │  Stream A (More Tests)          │
│  ┌──────┐                       │ │  ┌──────┐  ┌──────┐            │
│  │PR #1 │──────────────────────────▶│PR #2 │──│PR #3 │            │
│  └──────┘                       │ │  └──────┘  └──────┘            │
│  테스트 인프라                    │ │  컴포넌트     API 테스트         │
│                                 │ │                                 │
│  Stream B (Core Refactoring)    │ │  Stream B (Optimization)        │
│  ┌──────┐  ┌──────┐            │ │  ┌──────┐  ┌──────┐            │
│  │PR #4 │──│PR #5 │            │ │  │PR #7 │──│PR #8 │            │
│  └──────┘  └──────┘            │ │  └──────┘  └──────┘            │
│  파서 통합   Rate Limiting        │ │  노드 팩토리   스타일 통합        │
│                                 │ │                                 │
│  Stream C (Bug Fixes)           │ │  Stream C (UX Improvements)     │
│  ┌──────┐                       │ │  ┌──────┐  ┌──────┐            │
│  │PR #6 │                       │ │  │PR #9 │──│PR #10│            │
│  └──────┘                       │ │  └──────┘  └──────┘            │
│  메모리 누수                      │ │  패널 분리    성능 최적화         │
│                                 │ │                                 │
└─────────────────────────────────┘ └─────────────────────────────────┘

Week 3                              Week 4
┌─────────────────────────────────┐ ┌─────────────────────────────────┐
│                                 │ │                                 │
│  Stream A (A11y + Testing)      │ │  Stream A (Final Testing)       │
│  ┌──────┐  ┌───────┐           │ │  ┌───────┐                      │
│  │PR #11│──│PR #14 │           │ │  │PR #15 │                      │
│  └──────┘  └───────┘           │ │  └───────┘                      │
│  접근성     Export 테스트         │ │  Audit 테스트                   │
│                                 │ │                                 │
│  Stream B (Stability)           │ │  Stream B (Cleanup)             │
│  ┌───────┐  ┌───────┐          │ │  ┌───────┐                      │
│  │PR #12 │──│PR #13 │          │ │  │PR #16 │                      │
│  └───────┘  └───────┘          │ │  └───────┘                      │
│  타입 안전    에러 핸들링          │ │  Deprecated 제거                │
│                                 │ │                                 │
└─────────────────────────────────┘ └─────────────────────────────────┘
```

### 병렬 스트림 정의

| 스트림 | 역할 | 담당 PR |
|--------|------|---------|
| **Stream A** | 테스트 커버리지 | #1, #2, #3, #14, #15 |
| **Stream B** | 리팩토링 & 안정성 | #4, #5, #7, #8, #12, #13, #16 |
| **Stream C** | 버그 수정 & UX | #6, #9, #10, #11 |

### 의존성 그래프

```
PR #1 ──┬──▶ PR #2 ──▶ PR #7
        │
        ├──▶ PR #3
        │
        ├──▶ PR #14
        │
        └──▶ PR #15

PR #4 ──▶ PR #10

PR #6 ──▶ PR #16

PR #8 ──▶ PR #9 ──▶ PR #11

PR #5 (독립)
PR #12 (독립)
PR #13 (독립)
```

---

## 9. 체크리스트

### Phase 1 완료 체크리스트

- [ ] PR #1: 테스트 인프라 구축 완료
- [ ] PR #2: 핵심 컴포넌트 테스트 완료
- [ ] PR #3: API 라우트 테스트 완료
- [ ] PR #4: 파서 통합 리팩토링 완료
- [ ] PR #5: Rate Limiting 추가 완료
- [ ] PR #6: 메모리 누수 수정 완료
- [ ] 테스트 커버리지 60% 이상 달성

### Phase 2 완료 체크리스트

- [ ] PR #7: 노드 팩토리 리팩토링 완료
- [ ] PR #8: 스타일 토큰 통합 완료
- [ ] PR #9: NodeDetailPanel 분리 완료
- [ ] PR #10: 성능 최적화 완료
- [ ] PR #11: 접근성 개선 완료
- [ ] 번들 크기 20% 감소

### Phase 3 완료 체크리스트

- [ ] PR #12: 타입 안전성 강화 완료
- [ ] PR #13: 에러 핸들링 고도화 완료
- [ ] PR #14: Export 모듈 테스트 완료
- [ ] PR #15: Audit 모듈 테스트 완료
- [ ] PR #16: Deprecated 코드 제거 완료
- [ ] 테스트 커버리지 80% 이상 달성

### 최종 검증 체크리스트

- [ ] 전체 테스트 통과
- [ ] 빌드 성공
- [ ] Lighthouse 성능 점수 90+
- [ ] Lighthouse 접근성 점수 90+
- [ ] 메모리 누수 테스트 통과
- [ ] E2E 테스트 통과 (선택)

---

## 부록

### A. 참고 파일 위치

| 카테고리 | 파일 경로 |
|----------|----------|
| 프로젝트 설정 | `.claude/CLAUDE.md` |
| 인프라 데이터 규칙 | `.claude/rules/infra-data-rules.md` |
| 인프라 데이터 에이전트 | `.claude/agents/infra-data-agent.md` |
| 장비 현황 문서 | `docs/INFRASTRUCTURE_COMPONENTS.md` |
| 레퍼런스 분석 | `reports/REFERENCE_ANALYSIS_REPORT.md` |

### B. 명령어 참조

```bash
# 테스트 실행
npm test

# 커버리지 리포트
npm run test:coverage

# 빌드
npm run build

# 린트
npm run lint

# 타입 체크
npm run type-check
```

### C. 관련 이슈 트래킹

이 문서의 각 PR은 GitHub Issue로 생성하여 추적하는 것을 권장합니다:

```
Issue Template:
Title: [PR #N] {PR 제목}
Labels: enhancement, refactoring, testing
Milestone: v0.2.0
```

---

*이 문서는 InfraFlow 프로젝트 개선을 위한 마스터 플랜입니다.*
*다른 Claude 세션에서 이 문서를 참조하여 작업을 이어갈 수 있습니다.*

---

**문서 끝**
